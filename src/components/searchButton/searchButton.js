import React from 'react';

const searchButton = (props) => {
    return (
        <button type="button"
        className=""
        onClick={props.handleClick}
        />
    )
}

export default searchButton;